part of '../configuration_view.dart';

class _WorkspaceConfiguration extends StatelessWidget {
  const _WorkspaceConfiguration({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
